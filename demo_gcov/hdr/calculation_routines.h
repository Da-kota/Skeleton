
#ifndef CALCULATION_ROUTINES
#define CALCULATION_ROUTINES

#include <stdint.h>

extern uint8_t add(uint8_t x, uint8_t y);
extern uint8_t substract(uint8_t x, uint8_t y);
extern uint8_t multiply(uint8_t x, uint8_t y);
extern uint8_t divide(uint8_t x, uint8_t y);

#endif
